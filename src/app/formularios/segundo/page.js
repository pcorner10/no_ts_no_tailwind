"use client";

import { useState } from 'react';
import { SexoSelect, TrabajaSelect, FumadorSelect, NivelEscolarSelect, ParentescoSelect } from '../formularios';
import { segundoFormulario } from "../data"
import {Form, Row, Col, Label, Input, FormGroup, Button} from "react"
import 'bootstrap/dist/css/bootstrap.min.css';

export default function Encuesta2() {
  
  const [form, setForm] = useState(segundoFormulario);

  return (

  );
}
